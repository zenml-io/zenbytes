stack_components:
  artifact_store:
    s3_store: s3
  container_registry:
    ecr_registry: default
  metadata_store:
    kubeflow_metadata_store: kubeflow
  orchestrator:
    eks_orchestrator: kubeflow
  secrets_manager:
    local_secret_manager: local
stacks:
  aws_kubeflow_stack:
    artifact_store: s3_store
    container_registry: ecr_registry
    metadata_store: kubeflow_metadata_store
    orchestrator: eks_orchestrator
    secrets_manager: local_secret_manager
